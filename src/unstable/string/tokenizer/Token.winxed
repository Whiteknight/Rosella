namespace Rosella { namespace String { namespace Tokenizer
{
    class Token
    {
        var data;
        var type;

        function Token(string data, int type)
        {
            self.data = data;
            self.type = type;
        }

        function data()
        {
            return self.data;
        }

        function type()
        {
            return self.type;
        }
    }
}}}
